from typing import List
from late_classifier.features.core.base import FeatureExtractor


class {{extractor_name}}(FeatureExtractor):
    def get_features_keys(self) -> List[str]:
        # Fill the list with the names of the features that will be computed
        return []

    def get_required_keys(self) -> List[str]:
        # Fill the list with the names of the required columns
        return []

    def _compute_features(self, detections, **kwargs):
        """
        Parameters
        ----------
        detections :class:pandas.`DataFrame`
        DataFrame with detections of an object.

        kwargs Not required.

        Returns :class:pandas.`DataFrame`
        -------
        """
        ################################
        #   Here comes the Step Logic  #
        ################################
        return